<h1><%= @topics.title %></h1>

<% Bookmark.all.where(topic_id = @topics.id).each do |bookmark| %>
<% if bookmark.topic_id = @topics.id %>

<p><%= link_to bookmark.url, bookmark_path(bookmark.id) %></p>
<% else %>
<% end %>
<% end %>


